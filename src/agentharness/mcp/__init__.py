"""MCP client support: connect to configured MCP servers and expose their tools.

Note for readers: this package is ``agentharness.mcp``, while the official SDK it
builds on is the top-level ``mcp`` package. Imports are absolute (Python 3), so
``from mcp import ClientSession`` inside this package still reaches the SDK.
"""
